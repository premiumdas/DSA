#include <iostream>
#include <stack>
using namespace std;

bool matching(char a, char b){
    return (a=='(' && b==')') || (a=='{' && b=='}') || (a=='[' && b==']'); 
}

bool balance(string str){
    stack<char> s;
    for(char x : str){
        if(){
            return false;
        }
        
    }
}